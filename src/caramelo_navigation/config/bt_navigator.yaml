bt_navigator:
  ros__parameters:
    use_sim_time: false
    global_frame: map
    robot_base_frame: base_footprint
    odom_topic: /odom
    transform_tolerance: 0.3
    default_server_timeout: 20
    bt_loop_duration: 10
    wait_for_service_timeout: 1000
    action_server_result_timeout: 900.0
    error_code_names:
      - compute_path_error_code
      - follow_path_error_code

    # O launch injeta behavior_tree/caramelo_theta_dwb.xml como BT padrao.
    #
    # Plugins cadastrados para testes:
    # planner_id: GridBased, ThetaStar
    # controller_id: FollowPath, DWBHolonomic, MPPIHolonomic
    # smoother_id: simple_smoother, savitzky_golay_smoother
    #
    # caramelo_theta_dwb.xml fixa ThetaStar + DWBHolonomic.
    # simple_navigation*.xml ficam como exemplos/experimentos.
